package controller;

import java.io.IOException;
import java.net.MalformedURLException;
import java.util.ArrayList;

import org.jsoup.HttpStatusException;
import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;


public class Test {

	@SuppressWarnings("restriction")
	public static void main(String[] args) {
		

		
		
		
		/*
		 * String url = "http://localhost:8080/requests/status.xml";
		 * 
		 * String authString = ":p"; org.jsoup.nodes.Element line; String string =
		 * "blank"; String title = "<info name=\"title\">"; String format =
		 * "[artist] - [title] - [title] - [artist] Hello how are you ";
		 * 
		 * ArrayList<String> text = new ArrayList<>(); ArrayList<String> tags = new
		 * ArrayList<>();
		 * 
		 * tags.add("<info name=\"title\">"); tags.add("<info name=\"artist\">");
		 * 
		 * text.add("Artist Name"); text.add("Song Name");
		 * 
		 * title = new String(format);
		 * 
		 * title = title.replaceAll("\\[artist\\]", text.get(0)); title =
		 * title.replaceAll("\\[title\\]", text.get(1));
		 * 
		 * System.out.println(title);
		 */
		
	}
}
